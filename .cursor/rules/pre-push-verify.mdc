---
description: Run analyze/build checks before pushing oxplayer-client changes
globs: oxplayer-client/**/*
alwaysApply: false
---

# Pre-push verification (oxplayer-client)

**Normal pushes:** every `git push` should pass `scripts/verify-all.sh` (pre-push hook).

**Release pushes:** `scripts/release-client.sh` defaults to `OX_SKIP_VERIFY=1` — bump/tag/push first; **CI / Build OXPlayer is the verify gate**. Opt-in local verify with `--verify`. Coordinated: from `oxplayer-be` run `pnpm release:all -y "…"`.

**One-time setup after clone:**

```bash
bash scripts/install-pre-push-hook.sh
```

**Manual run (same check the hook uses):**

```bash
bash scripts/verify-all.sh
```

Hook source (versioned): `scripts/git-hooks/pre-push`. For release, prefer the release script (sets `OX_SKIP_VERIFY`) over `--no-verify`.

Playback and catalog UI hooks for OX belong in `lib/oxplayer/` with thin call sites in Fladder detail/playback files — do not rewrite upstream providers.

After server API fixes, confirm on a movie/episode with **2+ file variants**:
- Version picker appears next to Play
- Labels show quality/language (from `MediaSource.Name`)
- Playback starts for each selected variant
